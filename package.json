{
  "name": "sahayog-mern",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts for running the Sahayog Foundation frontend and backend together. Run `npm install` in frontend/ and backend/ separately first.",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
