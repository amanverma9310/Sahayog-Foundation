{
  "name": "sahayog-mern",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts for running the Sahayog Foundation frontend, backend, and admin panel together. Run `npm install` in frontend/, backend/, and admin/ separately first.",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND,ADMIN -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
